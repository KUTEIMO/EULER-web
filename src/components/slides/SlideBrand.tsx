/** Wordmark for slides — language-neutral (no Spanish tagline from SVG) */
export default function SlideBrand() {
  return (
    <div className="slide-brand-mark">
      <span className="slide-brand-glyph" aria-hidden>
        π
      </span>
      <span className="slide-brand-word">EULER</span>
    </div>
  );
}
